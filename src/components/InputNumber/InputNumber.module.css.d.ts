// このファイルは自動生成されています。直接編集しないでください。
declare const styles: {
  readonly inputNumber: string;
  readonly inputNumber__field: string;
  readonly inputNumber__spin: string;
  readonly inputNumber__spinButton: string;
  readonly inputNumber__spinDivider: string;
};
export default styles;
